import React from 'react';
import {Container, Header} from 'semantic-ui-react';

const Feed = props => {
	return (
		<Container>
			<Header>This is my app</Header>
		</Container>
	);
};

export default Feed;
